---
import { label } from '../lib/i18n';
import type { Lang } from '../lib/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
---

<article class="card">
  <h1>{label('performanceEmptyTitle', lang)}</h1>
  <p class="muted">{label('performanceEmptyBody', lang)}</p>
</article>
