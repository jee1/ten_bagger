repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.0
    hooks:
      - id: ruff
        args: [--fix, --config=scripts/ruff.toml]
        files: ^scripts/
      - id: ruff-format
        args: [--config=scripts/ruff.toml]
        files: ^scripts/
