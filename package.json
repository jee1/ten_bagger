{
  "name": "ten_bagger",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:daily": "cd scripts && python generate_daily.py",
    "regenerate:ledger": "cd scripts && python regenerate_ledger.py",
    "test:python": "cd scripts && python -m pytest tests/ -q",
    "validate:content": "cd scripts && python sync_manifest.py && python validate_content.py",
    "gen:types": "node scripts/gen_types.mjs",
    "gen:types:check": "node scripts/gen_types.mjs --check",
    "check": "npm run gen:types:check && astro check",
    "test:performance-ui": "node --experimental-strip-types --test src/lib/performanceAggregate.test.ts src/lib/performanceLoad.test.ts",
    "walk-forward": "cd scripts && python walk_forward.py",
    "walk-forward:smoke": "cd scripts && python -m pytest tests/test_walk_forward_smoke.py -q",
    "calibrate": "cd scripts && python calibrate.py",
    "calibrate:smoke": "cd scripts && python -m pytest tests/test_calibration_smoke.py -q"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.1.1",
    "json-schema-to-typescript": "^16.0.0",
    "typescript": "^6.0.3"
  }
}
